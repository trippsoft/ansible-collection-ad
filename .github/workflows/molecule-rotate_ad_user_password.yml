---
name: Molecule - rotate_ad_user_password role
'on':
  workflow_call: {}
  workflow_dispatch: {}
  pull_request:
    branches:
      - main
    paths:
      - galaxy.yml
      - roles/rotate_ad_user_password/**
      - '!roles/rotate_ad_user_password/.aar_doc.yml'
      - '!roles/rotate_ad_user_password/README.md'
      - '!roles/rotate_ad_user_password/meta/argument_specs.yml'
      - '!roles/rotate_ad_user_password/meta/main.yml'
  push:
    branches:
      - main
    paths:
      - galaxy.yml
      - roles/rotate_ad_user_password/**
      - '!roles/rotate_ad_user_password/.aar_doc.yml'
      - '!roles/rotate_ad_user_password/README.md'
      - '!roles/rotate_ad_user_password/meta/argument_specs.yml'
      - '!roles/rotate_ad_user_password/meta/main.yml'
defaults:
  run:
    working-directory: 'trippsc2.ad'
jobs:
  molecule:
    name: Run Molecule tests - rotate_ad_user_password
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.ad
      - name: Run Molecule tests
        working-directory: 'trippsc2.ad'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          rm -rf ~/.ansible/collections/ansible_collections/*
          cd ./roles/rotate_ad_user_password
          molecule test
          rm -rf ~/.ansible/collections/ansible_collections/*
          deactivate
        env:
          ANSIBLE_FORCE_COLOR: '1'
          PY_COLORS: '1'
          MOLECULE_BOX: ${{ matrix.box }}
    strategy:
      fail-fast: false
      matrix:
        box:
          - w2025_cis
          - w2022_cis
          - w2019_cis
